import './Hero.css'
import arrow-btn from "
function hero({heroData, heroCount, setHeroCount, playStatus, setPlayStatus})
  return (
    <div className='hero'>
      
    </div>
  )

export default hero
